- name: Configure scrape targets onto the observability stack
  hosts: observability
  collections:
    - kubernetes.core
  vars_files:
    - ../../vars/k3s.yml
    - ../../vars/scrape_profiles.yml
    - ../../scrape_configs.yml
  become: true
  tasks:
    - import_tasks: ../../tasks/configure-scrape-targets.yml
