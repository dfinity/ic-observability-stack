apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  namespace: observability-stack
  name: prometheus
  labels:
    app: prometheus
spec:
  endpoints:
    - interval: 30s
      port: web
  selector:
    matchLabels:
      app: prometheus
